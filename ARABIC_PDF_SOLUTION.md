# حل العربية في PDF

## المشكلة
عند تصدير PDF، النصوص العربية لم تكن تظهر بشكل صحيح لأن ReportLab يحتاج إلى خط يدعم العربية.

## الحل المطبق

### 1️⃣ مدير الخطوط الجديد (`font_manager.py`)
- ✅ يبحث عن خط عربي على النظام تلقائياً
- ✅ يسجل الخط في ReportLab
- ✅ يوفر وظائف للحصول على اسم الخط المناسب

### 2️⃣ خطوط الدعم
يدعم الحل الخطوط التالية تلقائياً:

**على Windows:**
- `C:\Windows\Fonts\arial.ttf` ✓
- `C:\Windows\Fonts\calibri.ttf`
- `C:\Windows\Fonts\times.ttf`

**على Linux (PythonAnywhere):**
- `/usr/share/fonts/truetype/noto/NotoSansArabic-Regular.ttf`
- `/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf`
- `/usr/share/fonts/truetype/liberation/LiberationSans-Regular.ttf`

### 3️⃣ كيفية العمل

#### Step 1: تسجيل الخطوط (عند بدء التطبيق)
```python
from .font_manager import register_arabic_fonts, get_arabic_font_name, get_arabic_font_bold

# تسجيل الخطوط
register_arabic_fonts()
```

#### Step 2: استخدام الخط في PDF
```python
# الحصول على اسم الخط المناسب
title_font = get_arabic_font_bold()      # للعناوين الغامقة
body_font = get_arabic_font_name()       # للنصوص العادية

# استخدامها في التنسيق
title_style = ParagraphStyle(
    'Title',
    fontName=title_font,
    # ...
)
```

#### Step 3: معالجة النصوص العربية
```python
# استخدام reshape_arabic_text لتحويل النصوص
text = reshape_arabic_text('النص العربي')
```

## التوافقية

### ✅ Windows
- يبحث عن Arial الذي يدعم العربية بشكل جيد
- يعرض النصوص العربية بشكل صحيح

### ✅ Linux (PythonAnywhere)
- يبحث عن NotoSansArabic أو DejaVuSans
- يدعم النصوص العربية بشكل كامل
- بدون الحاجة لتنزيل أو تثبيت خطوط إضافية

### ⚠️ الخيار الاحتياطي
إذا لم يوجد أي خط عربي على النظام:
- سيستخدم Helvetica كبديل
- قد لا تظهر النصوص العربية بشكل صحيح
- لكن التطبيق سيعمل بدون أخطاء

## الملفات المعدلة

1. **`car_app/font_manager.py`** (جديد)
   - مدير الخطوط الجديد
   - يتولى تسجيل وإدارة الخطوط

2. **`car_app/views.py`**
   - استيراد وظائف مدير الخطوط
   - تحديث `export_cars_pdf()` و `export_sales_pdf()`
   - استخدام الخطوط المسجلة في التنسيق

## الاختبار المحلي

عند تشغيل الخادم محلياً:
```
✓ وجدت الخط: C:\Windows\Fonts\arial.ttf
✓ تم تسجيل الخط: C:\Windows\Fonts\arial.ttf
```

## الاختبار على PythonAnywhere

1. سيبحث عن الخطوط على نظام Linux
2. إذا وجد NotoSansArabic أو DejaVuSans، سيسجله
3. سيعرض النصوص العربية بشكل صحيح

## الخلاصة

✅ الحل جاهز على:
- Windows (محلياً)
- Linux/PythonAnywhere (على الخادم)

🎯 النصوص العربية ستظهر بشكل صحيح في PDF على كلا النظامين!
